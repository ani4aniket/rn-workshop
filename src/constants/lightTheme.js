import color from './color';

const lightTheme = {
  backgroundColor: color.muted,
  textColor: color.black,
  textColorMuted: color.muted,
  touchEffect: color.lightBlue,
  headerBackgroundColor: color.pureWhite,
  cardBackgroundColor: color.pureWhite,
  ...color,
};

export default lightTheme;
